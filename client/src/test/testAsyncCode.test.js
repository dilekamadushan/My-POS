/*
// jest.mock('./http');

const {userInfo} = require('../actions/userActions');

test('should return user info', () => {
    userInfo('123').then(user => {
        expect(user).toBe('DELECTUS AUT AUTEM');
    });
});

const {loadTitle} = require('../components/utils');

test('should print an uppercase text', () => {
    loadTitle().then(title => {
        expect(title).toBe('DELECTUS AUT AUTEM');
    });
});*/
